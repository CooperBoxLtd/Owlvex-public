# SQL Query Corpus

This corpus seeds the second deterministic reasoning axis for Owlvex:

- SQL and contextual query execution

The first pack is intentionally small.
It exists to define clear ground truth before engine implementation starts.

Current focus:

- direct SQL string interpolation
- parameterized query negatives
- trust propagation into query text
- wrapped query helpers
- SQL context mismatch
